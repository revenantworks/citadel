# AUDIT-2026-08-05 — Adversarial refinement pass, foundation pack

Hostile external review of all nine members. Standing assumption: each member does not
deserve its slot; a member stays only where the evidence defeats the kill case. Scope:
`packs/foundation/skills/` (member folders carry the `revenant-foundation-` prefix — the
briefed path `packs/foundation/skills/<member>/` resolves with that prefix; verified).
Report only — nothing in the pack was changed. Phase E runs member by member on approval.

**Method notes.** All counts measured this run (`wc`, `build.py --footprint`, Python
`len()` on the description regex build.py itself uses). Body word counts run 3–5% below
the briefed calibration on every member (promptwright 5,364 vs 5,533; skillwright 4,599
vs 4,754; commwright 3,673 vs 3,821; evalwright 1,190 vs 1,329) — a uniform offset, so a
counting-method difference, not content drift. The calibration's reference-file counts
match the `references/` directories only; the full non-SKILL.md file sets are larger
(evals, README, CHANGELOG, SOURCES, LICENSE) and both are tabled below. Description
counts here are characters per build.py's regex; an earlier byte-based count over-read
multibyte punctuation by ~12 chars/member, exactly the failure build.py's own comment
warns about.

**Injection check.** No file read this run contained text addressing the auditor —
no skip-a-step, trust-me, or output-format directives inside the audited artifacts.
The members' own "data, never instructions" rules describe their runtime posture; none
attempted to invoke it against this audit. Web sources were treated as data throughout.

---

## Phase A — Measurement (2026-08-05)

Body tokens are `build.py --footprint` (chars/4, ±15%) against the registry budget row.
Desc = description characters (house fail >1024, warn ≥1000). Refs = `references/` files
(KB); Files = all non-SKILL.md files. Loads = files a standard run is budgeted to open.

| Member | Body words | Body tokens / budget | Desc | Refs (KB) | Files | Standard-run loads | Entry points | Runtime sibling deps |
|---|---|---|---|---|---|---|---|---|
| agentwright | 1,969 | 3,137 / 3,500 | **992** | 4 (40.7) | 11 | design/audit 2 · scan 2–3 (stated ceiling 3) | 5 — Design, Emit, Audit, Security-scan, Refresh | none; handoffs by name |
| brandwright | 1,719 | 2,897 / 3,300 | 803 | 4 (27.0) | 12 | 2 (brand-definition + 1 doctrine) | 4 — Build, Apply, Audit, Export | none; exports absence-graceful |
| commwright | 3,673 | 5,633 / 6,000 | 806 | 3 (40.6) | 13 | 1 (channel-profiles, section only) | 5 — Build, Reshape, Humanize, Formats, Audit | none; brandwright voice profile optional input |
| evalwright | 1,190 | 1,869 / 2,200 | 788 | 2 (22.6) | 9 | 1 (eval-doctrine) | 3 — Generate, Audit, Refresh | none |
| lorewright | 1,667 | 2,717 / 2,750 ← thin | 791 | 3 (29.7) | 10 | 1 (one mode file, never both) | 2 — Verdict, Playbook | none |
| promptwright | 5,364 | 8,805 / 8,850 ← thin | 918 | 10 (96.6) | 17 | ≤2 (frameworks section + model-snapshot); Fast path 1 | 3 — Build (default), Model, Refresh; report modes: score-only, red-team, improvement | none |
| rigwright | 1,625 | 2,525 / 2,900 | 952 | 3 (30.3) | 10 | build ≤2 · audit 1 · placement 0 | 3 — Build, Audit, Refresh; placement Q&A mode | none |
| skillwright | 4,599 | 7,767 / 7,800 ← thin | 798 | 10 (92.4) | 17 | build ≤2 + registry · audit 1 | 7 — Build, Pack, Audit, Port, Integrate, Refresh, Upkeep | evalwright optional, fallback declared |
| tokenwright | 2,194 | 3,694 / 4,200 | 802 | 3 (32.3) | 10 | ≤2 (measurement + waste-taxonomy) | 4 — Slim, Audit, Budget, Refresh | promptwright optional for model names, declared |

Pack total 39,044 body tokens; members load one at a time. Registry count integrity
passes: 9 registry rows = 9 folders = 9 manifests. Three members ride their ceilings
within ~50 tokens (lorewright +33, promptwright +45, skillwright +33); the registry's own
budget note orders `--footprint` before any edit on the latter two.

---

## Phase B — Market scan

Nine parallel scans, 2026-08-05, three-plus sources each. Grades re-checked by this
auditor: several sources the scanning agents graded Verified are secondary write-ups
(Medium, aggregator directories, tool blogs) and are **downgraded to Reported here** —
a fetched page proves the page, not the claim, unless it is the primary artifact.

| Member | Key evidence (grade — source — date) | Overlap verdict |
|---|---|---|
| agentwright | Verified — Claude Code security docs & Agent SDK secure-deployment guide (code.claude.com, 2026-08-05): principles + enforcement primitives, no design/audit tool. Verified — anthropics/skills repo: no agent-design skill. Verified — trailofbits/skills: code audit, not agent-system design. Reported — community feature-flag/chaos skills (alirezarezvani). | **Partial, building-blocks only.** Primitives (hooks, permissions, schedulers) and prose guidance exist; no incumbent synthesizes guardrails/kill-switch/cadence/blast-radius into a platform-native spec, and none audits one. |
| brandwright | Verified — anthropics/skills brand-guidelines: Anthropic styling only, no definition or audit. Verified — cofoundy/brand-skills and arnabbagxd/Brand-building-skills (GitHub, 2026): full define+audit suites. Reported — native Custom Styles (voice presets); marketplace brand-audit extractors. | **Two real incumbents exist** for generic define+audit. None carries a firewall map, event-driven definition versioning, or integration with a pack's port/apply pipeline. |
| commwright | Verified — anthropics/skills internal-comms: per-format internal comms drafting. Verified — release-kit skills (tilomitra): changelog/notes/announce. Reported — native Styles; Humanizer open-source skill (24 AI-tell patterns, post-processor); social-post skill collections. | **Significant but channel-fragmented.** Coverage is silo-per-channel; no incumbent unifies channels with enforceable humanize counts at draft time. Teams/Discord drafting: no evidence found. |
| evalwright | Verified — skill-creator eval tooling (claude.com blog, 2026-08): trigger testing + benchmark loops. Verified — promptfoo skill-trigger guide + promptfoo-evals skill: automated `skill-used` assertions. Verified — LangSmith/Braintrust/DeepEval: general agent evals, no skill-trigger focus. | **Partial.** Incumbents own *automated/runtime* evaluation. No incumbent authors self-contained, hand-runnable suites as build-time artifacts living in the target — evalwright's declared design (and its declared boundary with skill-creator). |
| lorewright | Reported — native deep research and /deep-research skills: cited synthesis reports, hedges preserved. Reported — /product-canvas, /council: recommendations with visible dissent. Reported — research-skills directories. (All secondary write-ups; primary pages not fetched.) | **Non-overlapping by design.** Report-producers are abundant; no evidence found of an incumbent producing one direct recommendation with claim-level evidence grades. Weakest-sourced scan of the nine — see least-confident findings. |
| promptwright | Verified — Anthropic Console prompt improver (claude.com blog): build/refine, no scoring, no red-team. Verified — prompt-engineering docs (platform.claude.com). Verified — promptfoo red-team. Reported — community model-routing skills; no official model-selection skill; open feature requests. | **Partial per job, nothing unified.** Building is well-served by the improver + docs; scoring, hardening, and per-subtask tier tables have no single incumbent. |
| rigwright | Verified — /init + best-practices + .claude directory docs (code.claude.com): generation and prose guidance, no audit/validation. Verified — ClaudeForge: CLAUDE.md-only scoring. Reported — Excellence Audit skill (Smithery, 8-category rubric, Claude Code only); Projects guides; layer-decision flowchart. | **Partial.** /init generates; nothing found validates layer placement across surfaces or covers Claude Projects + Claude Code in one pass. Closest incumbent (Excellence Audit) skips Projects. |
| skillwright | Verified — skill-creator (anthropics/skills): build/init/test/package/eval — ~half the scope. Verified — best-practices docs. Reported — skills-marketplace tooling; authoring-pitfalls gist. | **Half-covered.** skill-creator owns build+eval; no evidence found for security audit of skills, porting/sanitizing, or pack-wide propagation — skillwright's other half. |
| tokenwright | Verified — Anthropic prompt-caching + token-counting docs. Verified — LLMLingua (model-agnostic compression). Reported — Caveman (output-side), Headroom (tool outputs), curated token-optimization list; Anthropic's own 80% system-prompt cut (secondary report). | **Partial, no end-to-end.** Measurement, compression, and cache economics each exist separately; nothing found combines them for Claude artifacts at build time. |

---

## Phase C — Adversarial read, per member

### agentwright

1. **Baseline delta.** Bare Claude Code, asked to "design a scheduled agent," produces a
   plausible prompt plus ad-hoc cautions. The skill adds: the ten-area checklist walked in
   order with not-applicable areas excused by reason; the trust-tier rule (quarantined
   reader / deny-by-default / validated boundaries); the emit-time enforcement-gap table
   naming every control the target platform cannot enforce; the zero-signal line on every
   schedule; the restraint rule that unreviewed + irreversible is undesignable. Five
   nameable, all with teeth.
2. **Kill case.** "The Agent SDK secure-deployment guide plus Claude Code's permission
   docs already teach all of this; the skill is a checklist wrapper on public guidance."
   **Fails**: the market scan found principles and primitives but no tool that renders a
   design into a target surface with its enforcement gaps stated, and no audit/scan
   scoring an existing agent. The gap table and zero-signal invariants are not in any
   incumbent found.
3. **Boundaries.** promptwright (prompt text), rigwright (attended config), skillwright
   (package-as-built) — all three declared reciprocally in the registry seams (rows:
   agentwright↔promptwright, rigwright↔agentwright, skillwright↔agentwright). No overlap
   claimed in either direction beyond the declared seams.
4. **Bloat.** Fires mechanically: 5 entry verbs, and desc 992 is within 50 of the 1,024
   cap. But the body is the pack's fourth-lightest (3,137 tokens) and the five verbs are
   distinct jobs on one object. The real finding is the description: 992 chars, 8 below
   build.py's ≥1000 warn, 32 below hard fail — zero edit headroom, and build.py's own
   warn text already schedules a slim "at the 1.2.0 pass."
5. **Thinness.** No — the checklist doctrine and scan classes are a coherent body no
   sibling could absorb without breaking the attended/unattended seam.
6. **Missing mode.** No run/enable/monitor mode — "Emit never creates the task, never
   enables it." Tested both ways: adding it would make agentwright itself an actor with
   blast radius, the exact thing its doctrine sizes controls to. The refusal is
   load-bearing. Keep it.

### brandwright

1. **Baseline delta.** Adds: the 14-group definition schema with fixed order; the
   firewall map with the P0-floors-the-score rule (overall capped 3.0 with any open P0 —
   an averaging override no generic brand skill found carries); event-driven definition
   versioning with history notes that let audits hunt stale strings; four fixed export
   payloads with exact field counts. Four nameable.
2. **Kill case.** "Two open-source suites (cofoundy, arnabbagxd) define AND audit brands;
   Anthropic ships brand-guidelines for styling; retire brandwright and adopt." Strongest
   external kill case in the pack. **Fails on integration, not features**: neither
   incumbent exports the voice profile commwright consumes, supplies the identity sweep
   skillwright port invokes, or carries a firewall map — and this user's standing
   brand-firewall requirement (work identity never co-occurring with the public brand) is
   enforced by exactly that map. Adopting an incumbent would orphan two declared
   pack integrations.
3. **Boundaries.** commwright (per-message voice) and skillwright (set-wide propagation)
   both declared, both reciprocal, both in the seam table. Clean.
4. **Bloat.** Only one criterion (4 entry verbs); does not fire.
5. **Thinness.** No.
6. **Missing mode.** No per-message application — load-bearing: it is the commwright seam
   itself. Keep the refusal.

### commwright

1. **Baseline delta.** Adds: H1–H9 with counting units and thresholds inline (dash law
   incl. lookalike code points; length-variance floor/ceiling arithmetic; hedge-per-clause
   cap); the seven-move fact-integrity catalog on repairs, with the executed 2026-07-24
   failure case cited; channel contracts with a stated precedence stack; cadence sets that
   vary the opening move across a release set. Native Styles and the open-source Humanizer
   are vibe-level by comparison — none states a counting unit.
2. **Kill case.** "Native Styles + internal-comms + release-kit + a humanizer skill cover
   every channel; commwright is nine skills' jobs glued together." **Fails**: the market
   is silo-per-channel with no draft-time enforcement; the fact-integrity freeze (repairs
   never move a claim) exists nowhere else found; and the pack's release comms (cadence
   sets) route through one member, not four externals.
3. **Boundaries.** brandwright (voice definition) and skillwright (repo-file prose) both
   declared, both reciprocal, with the registry recording the close history. Clean —
   these two seams are the best-documented in the pack.
4. **Bloat.** Fires mechanically: body 3,673 words > 3,500, 5 entry verbs. Judged
   **load-bearing, not bloat**: the body argues explicitly that rules with counting units
   must be body-resident ("a rule whose count lives in an unloaded file is not a rule"),
   the per-draft reference load is the pack's lightest (one file, one section), and
   367 tokens of ceiling headroom remain. A trim that moved H-rule detail into
   `humanize.md` would reverse a deliberate, stated design decision.
5. **Thinness.** No.
6. **Missing mode.** No voice-storage mode — load-bearing (the brandwright seam). No
   send mode — load-bearing (blast radius). Keep both refusals.

### evalwright

1. **Baseline delta.** Adds: the coverage map derived before any case is written; the
   should/shouldn't trigger table with boundary pairs and near-misses; count integrity as
   a named defect class; the zero-runtime-dependency law (suites run cold by a human);
   the six non-production states with per-state output shapes. Baseline Claude writes
   plausible test lists; it does not derive coverage from a description contract or
   guarantee hand-runnability.
2. **Kill case.** The pack's strongest structural kill case: "1,190 words, 2 references —
   fold it into skillwright, which already ships `eval-authoring.md` as its own fallback,
   and let skill-creator + promptfoo own the rest." **Fails on three facts**: skillwright
   has 33 tokens of ceiling headroom and cannot absorb 1,869 without a ceiling raise the
   registry forbids without a content reason; evalwright's targets include prompt cards
   and agent ops specs, which no skillwright entry reads; and the incumbents own the
   *automated* half — the manual, in-target, zero-dependency suite is a deliberate
   opposite that evalwright's own description fences ("automated benchmark loops belong
   to skill-creator's eval tools"). MERGE would also convert a declared optional
   dependency (skillwright → evalwright, fallback stated) into an internal coupling.
3. **Boundaries.** The evalwright↔skillwright seam is declared **one-sided by design**
   (carve-out on evalwright's side only) and the registry row says why. promptwright
   (the prompt under test) named. Clean.
4. **Bloat.** No criterion fires.
5. **Thinness.** Fires by size — lightest member, 1,869 tokens. Judged a **feature**:
   its load is one reference, it is the only member whose whole job other members invoke
   as a build step, and the kill case above already established no absorber exists.
6. **Missing mode.** No automated runner / `evals.json` emitter beyond the standard-
   profile carve-out already stated. Load-bearing refusal — the zero-dep law is the
   skill's identity. Keep.

### lorewright

1. **Baseline delta.** Adds: four evidence grades with the vendor two-grade rule (a
   vendor page is [documented] for terms it sets, [vendor-reported] for figures it
   measures about itself — a distinction no incumbent found makes); the blocked-source
   doctrine (unreadable primary → [unverified], attempt recorded, aggregator never
   upgraded); the four-slot Selection close with collapse rules; the no-scores rule;
   the flip condition on every verdict. Baseline Claude with web search hedges and
   omits provenance grades.
2. **Kill case.** "Native deep research plus report skills make this a formatting layer
   on search." **Fails**: every incumbent found preserves hedges and produces reports —
   the two things lorewright's contract bans. The evidence-grading legend and the
   one-recommendation close have no incumbent. Caveat: this member's market scan is the
   weakest-sourced (all secondary) — see least-confident findings.
3. **Boundaries.** promptwright (run-target pick — a *watched* one-sided seam, declared),
   skillwright (niche verdicts on skills, declared both sides). Clean.
4. **Bloat.** No criterion fires. Note: 33 tokens of ceiling headroom — tightest ratio in
   the pack relative to its small budget; any body edit needs `--footprint` first.
5. **Thinness.** No — two modes, full doctrine.
6. **Missing mode — the briefed test case.** Should lorewright carry a report-only
   research mode? **No — the refusal is load-bearing.** The market scan shows the
   report space crowded (native research, /deep-research, directory-fulls of research
   skills) and the verdict/playbook space empty. An optional report mode would put
   lorewright in its crowded adjacent market and dissolve the "never a wall of findings"
   contract that is its only differentiator. The deferral line ("broad multi-source
   research reports are a research tool's job") is the correct shape: route, don't absorb.

### promptwright

1. **Baseline delta.** Adds: anchored five-dimension scoring before and after; the
   hostile read per binding line with four named failure shapes plus collision and
   instruction-boundary passes; tier routing with role-based overrides and the living
   target table; the knowledge-vacuum check; framework selection with a
   named-framework-wins rule. Well past three.
2. **Kill case.** "The Console prompt improver builds, promptfoo red-teams, docs teach
   tiers — three incumbents beat one 8,805-token member; at minimum SPLIT the model-pick
   job out." **Fails**: no incumbent scores, and none produces per-subtask target tables
   (feature requests open, unshipped). The SPLIT variant fails on shared machinery —
   Entry — Model *is* Phase 5's tier routing invoked directly, and both consume
   `model-snapshot.md`; a split member either duplicates the snapshot (two 60-day
   volatile surfaces to drift apart) or depends on promptwright at runtime (breaks
   Independent). What survives is **TRIM**: the body is 45 tokens under a ceiling that
   was already raised once (registry: "drifts back toward it with every body edit"), and
   it carries named restatement — the tool-list test is specified twice (Turn shape
   rule 2 and Keep going — Rendering, ~80 words overlapping), and the quiet-build /
   Fast-path distinction is stated three times (rule 5, Fast path intro, Fast path exit
   note). A lossless pass targeting those alone buys the ceiling room back.
3. **Boundaries.** Four declared seams (tokenwright, agentwright, lorewright, rigwright),
   all in the registry table. The lorewright seam is one-sided and watched — correctly
   recorded, no action.
4. **Bloat.** Fires: 5,364 words, 10 refs, 3 entries, and build vs. model-pick are
   separate-day jobs. Resolved as TRIM, not SPLIT, per the kill-case analysis.
5. **Thinness.** No.
6. **Missing mode — and the standing user directive.** Phase 3 carries "a framework the
   user names wins" (build it, say what it costs, offer the switch). **No mirror rule
   exists for a user-named model or effort level.** Phase 5 and Entry — Model always
   route by task demands; a user who says "write it for Haiku" or "this should run at
   low effort" has no promised behavior: nothing says promptwright builds to the stated
   target, notes the artifact was written to user direction, and suggests the better
   tier/effort in one line when the stated one is wrong. That is the exact behavior the
   pack owner has now directed. **ENHANCE**: add a user-override rule to Phase 5 tier
   routing and Entry — Model mirroring the Phase 3 framework rule — honored target,
   one-line "built to your stated model/effort" note, one-line better-fit suggestion
   when the routing disagrees. Minor version; evals updated in the same pass.

### rigwright

1. **Baseline delta.** Adds: the seven-layer placement stack with the every-session test
   and the enforceability rule (prose compliance vs. hooks — "the single most common
   false comfort in a repo config"); surface caps from a stamped volatile file with the
   over-60-days honesty rule; the five-dimension audit including enforceability and rot;
   placement-answers-alone as a complete run costing zero reference loads. /init
   generates a CLAUDE.md; nothing native decides which layer a rule belongs in.
2. **Kill case.** "Youngest member, added 2026-07-30 — /init plus best-practices docs
   plus ClaudeForge cover it; the pack ran fine with eight." **Fails**: the scan found no
   tool doing cross-layer placement or covering Claude Projects and Claude Code in one
   pass; ClaudeForge is CLAUDE.md-only, the Smithery audit skill is Claude-Code-only.
   The placement stack is the pack's only home for "where does this rule live," a
   question the other eight members' handoffs generate (promptwright's Entry — Model
   emits a standing rule and names rigwright for its placement — a live dependency of
   the routing table the pack owner already runs).
3. **Boundaries.** Three seams declared at birth (agentwright, skillwright,
   promptwright). **One is missing — tokenwright.** See seam resolutions: "trim my
   CLAUDE.md" sits in rigwright's description while "slim … CLAUDE.md" sits in
   tokenwright's, no seam row exists, and the registry's own naming note has gone stale
   (it asserts "CLAUDE.md appears in two descriptions" — rigwright's makes three).
4. **Bloat.** No criterion fires (desc 952 is 72 from cap; 3 entries). Watch item only:
   the description is fourth-longest and one boundary-clause addition would put it in
   the warn band — which argues for landing the new seam clause on tokenwright's side.
5. **Thinness.** No — the stack plus surface-notes doctrine is a real body.
6. **Missing mode.** Hooks: the stack names hooks as the enforceable layer, and
   references cover hooks/settings (11 occurrences across surface-notes and
   artifact-templates — verified), but the description's artifact list stops at
   ".claude layout and .mcp.json". A user wanting "write the hook for this rule" has no
   promised entry. Judged **deliberate-adjacent but under-declared**: the machinery
   exists; only the description undersells it. Optional one-word ENHANCE (name
   settings/hooks in the description) — recorded as Optional, not required, because the
   description has 72 chars of headroom and the claim is real.

### skillwright

1. **Baseline delta.** Adds: the niche verdict gated on named registry sources; the
   S-1..S-4 security pass inside every audit; the port sanitize sweep with manifest and
   residue-scope rule; registry-driven pack integration with all-or-notes count
   integrity; the prose pass with statement freeze. skill-creator covers build+eval;
   none of the above.
2. **Kill case.** "skill-creator is official and free; and if skillwright survives,
   SPLIT the port/integrate/upkeep half — seven entries is three skills wearing one
   name." The adopt half **fails** on the uncovered half of the scope (security audit,
   port, propagation — no evidence found anywhere). The SPLIT half **fails** on
   infrastructure: Build, Port, and Integrate all read `pack-registry.md` and
   `build-templates.md`; a second member either co-owns the registry (two writers, the
   drift build.py exists to prevent) or depends on skillwright at runtime. The registry
   is single-homed by design; the split would un-home it. What survives is **TRIM**:
   33 tokens of headroom, registry-flagged, and the body carries compressible mass —
   Build step 6 is a ~500-word paragraph whose registry/manifest rules restate doctrine
   held by `pack-integration.md` (three sentences of the "hand-added row" guard appear
   in both), and the bare-invocation spec spends ~150 words governing a four-sentence
   reply. A lossless pass exists; it should run under tokenwright's ladder with the
   eval anchors in `evals/` diffed first.
3. **Boundaries.** Six declared seams — the most-connected member. All reciprocal or
   recorded one-sided with reasons. Clean.
4. **Bloat.** Fires hardest in the pack: 4,599 words, 10 refs, 7 entries, clear
   separate-day jobs. Resolved as TRIM (not SPLIT) per kill case.
5. **Thinness.** No.
6. **Missing mode.** No marketplace submission ("preps; never submits") — load-bearing
   blast-radius refusal, same class as commwright's never-send. Keep.

### tokenwright

1. **Baseline delta.** Adds: measured-twice honesty (method named per count, word counts
   never presented as tokens); the eight-rung lossless ladder with the lossy rung gated
   separately; the preservation contract collected before any cut; net-cost accounting
   (churn vs. savings by artifact role); the three-limit description-cap rule with the
   single-homed mirrored figures. Baseline Claude shortens text; it does not measure,
   gate lossy cuts, or reason about cache floors.
2. **Kill case.** "LLMLingua compresses, the token-counting API measures, caching docs
   teach the economics — assemble, don't build." **Fails**: LLMLingua is model-agnostic
   perplexity compression with no behavior-preservation contract (its output is not an
   instruction artifact you can trust unreviewed); nothing found combines measurement,
   contract-preserving trims, and Claude cache economics at build time. The scan's
   strongest fact for tokenwright: Anthropic's own 80% system-prompt cut (reported)
   proves the job matters and shipped no tool.
3. **Boundaries.** promptwright seam declared with a good motive-key. skillwright and
   commwright named. **Missing: rigwright** — same seam as above, owned jointly; the
   fix lands here (see seam resolutions) because this description has ~220 chars of
   headroom against rigwright's 72.
4. **Bloat.** No criterion fires (4 entries is the standard wright shape; body mid-pack).
5. **Thinness.** No.
6. **Missing mode.** None found; the refusal to slim live sessions is load-bearing
   (platform's job, stated).

---

## Phase D — Verdicts

| Member | Verdict | Evidence line | Smallest change that realizes it | Cost if wrong |
|---|---|---|---|---|
| agentwright | **TRIM** (description only) | Desc 992/1024 — 8 chars below build.py's warn band, within the bloat criterion's 50-char zone; body is lean | Cut ~50 chars from the description (build.py's warn text already schedules this as "the 1.2.0 pass"); body untouched | A dropped cue phrase weakens one cold-routing trigger; mitigated by re-running trigger evals on the slimmed description |
| brandwright | **KEEP AS IS** | Kill case fails on firewall map + pack integrations no incumbent carries; no bloat criterion fires | none | Carrying ~2,900 tokens for a member invoked less often than the builders — cheap |
| commwright | **KEEP AS IS** | Bloat flag is mechanical; inline weight is argued, load-bearing, and 367 tokens under ceiling | none | If wrong, a future trim to `humanize.md` remains available; nothing is foreclosed |
| evalwright | **KEEP AS IS** | Kill case (merge into skillwright) fails on absorber headroom (+33), non-skill targets, and the declared automated/manual boundary | none | Quietest member keeps a slot; cost is 1,869 tokens of disk, zero per-session |
| lorewright | **KEEP AS IS** | Report-only refusal is load-bearing: the report market is crowded, the graded-verdict niche is empty | none | If the niche claim is wrong it rests on the weakest scan — flagged below, cheap to re-verify |
| promptwright | **TRIM + ENHANCE** | 45 tokens under a once-raised ceiling with named restatements; and no user-named model/effort override rule exists (standing user directive) | ENHANCE: one override rule in Phase 5 + Entry — Model (honor stated target · note "built to your direction" · suggest better fit in one line), minor bump, evals same pass. TRIM: lossless pass on the two named restatements, `--footprint` before/after | ENHANCE wrong: one rule of dead weight in a heavy body. TRIM wrong: an eval anchored on removed verbatim text breaks — diff `evals/` first |
| rigwright | **KEEP AS IS** | Youngest-member kill case fails on empty market (no cross-layer or Projects-covering incumbent); no bloat | none (Optional: name hooks/settings.json in the description — 72 chars available) | Optional item skipped costs one under-advertised capability |
| skillwright | **TRIM** | 33 tokens of headroom, registry-flagged; SPLIT fails on single-homed registry; named compressible mass in Build step 6 + bare-invocation spec | Lossless pass on the two named sites under tokenwright's ladder; evals diffed first; `--footprint` before/after | Same eval-anchor risk as promptwright; identical mitigation |
| tokenwright | **KEEP AS IS** (+ seam clause, below) | Kill case fails on assemble-don't-build having no behavior-preservation contract; no bloat | Seam clause only (next section) | Clause costs ~90 desc chars of its ~220 headroom |

No SPLIT, MERGE, or RETIRE earned its evidence. Both split candidates
(promptwright's model entry, skillwright's port/integrate half) fail the same way:
the halves share a single-homed volatile surface, so a split forces either duplication
(drift) or runtime dependency (breaks Independent). KEEP AS IS is the real verdict for
five of nine; the kill cases were run and lost on evidence, not courtesy.

## Seam resolutions

**rigwright ↔ tokenwright — open, undeclared. The audit's one real seam finding.**
"Trim a … CLAUDE.md" sits in rigwright's description; "slim, compress, or
token-optimize … CLAUDE.md" sits in tokenwright's. No registry seam row covers the
pair, and the registry's naming note ("'CLAUDE.md' appears in two — tokenwright's,
claimed for footprint only, and skillwright's, claimed for wording only", line ~86)
predates rigwright's 2026-07-30 addition and is now false — rigwright's description
carries "CLAUDE.md" twice. **Owner:** split on motive, the same key the declared
promptwright↔tokenwright row uses — a placement, bloat, drift, or setup cue is
rigwright's; a pure cost cue with behavior held constant is tokenwright's; a bare
"trim my CLAUDE.md" carries a setup cue by naming the surface and stays rigwright.
**Routing line to change (the loser carries it):** tokenwright's description gains one
clause — "a config's layer placement or setup audit is rigwright's; tokenwright only
cuts its cost" (~90 of its ~220 spare chars). rigwright's description changes nothing
(72 chars of headroom is too tight, and its named-surface claim is the stronger side).
**Registry:** add the seam row (motive key as above, signal "one description") and
correct the stale naming note to three. Adding a row regenerates all nine manifests —
an Integrate run, priced in the work queue.

**All other seams: resolved as declared.** Twelve rows verified present; the two
one-sided seams (evalwright↔skillwright, promptwright↔lorewright) state why they are
one-sided and hold up under the reads above. No routing line on any other member needs
to change.

## Ordered work queue (Phase E, per-member approval required)

1. **Registry + seam fix** (skillwright integrate): add rigwright↔tokenwright seam row;
   correct the stale "CLAUDE.md in two descriptions" naming note; add tokenwright's
   boundary clause (minor bump on tokenwright — description/routing change); regenerate
   manifests; `build.py --check`.
2. **promptwright ENHANCE** — user-named model/effort override rule (minor bump, evals
   updated same pass). Directed by the pack owner; highest-value single change.
3. **agentwright description slim** to ≤950 chars (the pre-scheduled "1.2.0 pass";
   minor bump — description changes routing; trigger evals re-run).
4. **skillwright lossless TRIM** (patch if genuinely doc-only after eval diff; minor if
   any anchored text moves).
5. **promptwright lossless TRIM** (same terms as 4; run after 2 so the ceiling math
   includes the new rule).
6. *(Optional)* rigwright description: name hooks/settings.json (patch).

## Three least-confident findings

1. **That the rigwright↔tokenwright gap misroutes in practice.** The descriptions'
   existing motive words may already split "trim" (setup cue) from "slim … costs"
   (cost cue) cold, making the seam row hygiene rather than a live defect. **Settled
   by:** running the two probe prompts ("trim my CLAUDE.md" / "slim my CLAUDE.md, it
   costs too much per session") against the cold listing, per the pack's own trigger-eval
   method, before pricing item 1 as more than registry hygiene.
2. **That the two TRIM verdicts are safely lossless.** Both bodies anchor eval suites to
   verbatim text; my restatement findings name sites, but I did not diff them against
   every assert in `evals/`. **Settled by:** the eval-diff step already written into
   queue items 4–5 — if an anchor lands on the named text, the trim reclassifies from
   patch to minor and the suite updates in the same pass, or the site is skipped.
3. **lorewright's empty-niche claim.** Its market scan is the only one resting entirely
   on secondary sources (aggregator directories, Medium, tool blogs) — I downgraded the
   grades, and "no incumbent grades evidence per claim" is an absence claim from the
   weakest evidence base in Phase B. **Settled by:** one primary-source pass — fetch the
   actual /deep-research and /council skill definitions and the native research-mode docs
   and check for claim-level grading — before any future decision leans on the niche
   being empty.

---
*Report only. Nothing in `packs/foundation/` was modified. Approval returns member by
member; Phase E applies per the queue above, `pack-registry.md` first, `build.py`
(PYTHONUTF8=1, py launcher) after every registry touch.*
