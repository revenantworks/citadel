# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~
.*.swp
.idea/
.vscode/

# Python (skills ship no code, but keep local scratch out)
__pycache__/
*.pyc
.venv/
venv/

# Packaging artifacts — built on release, never committed
*.skill
*.zip
/dist/
/build/

# Local environment
.env
*.log

# Claude Code: track the firewall hook and neutral settings; the vocabulary
# (blocklist.txt) and local-only permissions (settings.local.json) never ship
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/hooks/blocklist.txt
.claude/settings.local.json
