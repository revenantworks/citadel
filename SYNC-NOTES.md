# Repo sync — 2026-07-14 (drop revenantworks.dev)

Unzip over the repo root, commit + push.

| File | Change |
|---|---|
| `brand/brand-definition.md` | Removed "· brand domain `revenantworks.dev`" from the Handles row (domain not being registered) |

Commit line:

    docs: drop revenantworks.dev from the public brand edition (domain not registered)

No skill files touched → no build.py, no version bump, no tag.

## Also (NOT in this bundle)
- Private brandsmith definition bumped v1.3.1 → v1.3.2: same domain reference dropped. Re-run the swap script with the v1.3.2 file, re-upload brandsmith to claude.ai.
- GitHub About panel: already clean (homepage/description/topics all unset on the live repo) — nothing to remove there.
